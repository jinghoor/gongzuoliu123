# 免费部署指南 🚀

本文档介绍如何将跨境电商工作流项目免费部署到各种平台。

## 📋 部署方案对比

| 平台 | 前端 | 后端 | 免费额度 | 推荐度 |
|------|------|------|----------|--------|
| **Vercel** | ✅ | ❌ | 无限 | ⭐⭐⭐⭐⭐ |
| **Netlify** | ✅ | ❌ | 100GB/月 | ⭐⭐⭐⭐⭐ |
| **Railway** | ✅ | ✅ | $5/月 | ⭐⭐⭐⭐ |
| **Render** | ✅ | ✅ | 750小时/月 | ⭐⭐⭐⭐ |
| **Fly.io** | ✅ | ✅ | 3个共享CPU | ⭐⭐⭐ |

## 🎯 推荐方案

### 方案一：分离部署（最推荐）⭐

**前端部署到 Vercel，后端部署到 Railway**

- ✅ 前端完全免费，无限流量
- ✅ 后端有稳定免费额度
- ✅ 性能好，速度快
- ✅ 配置简单

### 方案二：全栈部署

**前后端一起部署到 Railway 或 Render**

- ✅ 一个平台管理所有服务
- ⚠️ 免费额度有限制

---

## 🚀 方案一：分离部署（Vercel + Railway）

### 第一步：部署后端到 Railway

#### 1. 注册 Railway 账号
- 访问 https://railway.app
- 使用 GitHub 账号登录

#### 2. 创建新项目
1. 点击 "New Project"
2. 选择 "Deploy from GitHub repo"
3. 选择你的项目仓库
4. Railway 会自动检测项目

#### 3. 配置后端服务
1. 在项目设置中，设置根目录为 `backend`
2. 设置启动命令：`npm start`
3. 设置构建命令：`npm install && npm run build`
4. 添加环境变量：
   - `NODE_ENV=production`
   - `PORT=1888`（Railway 会自动分配端口，但可以设置）

#### 4. 获取后端 URL
- Railway 会自动分配一个 URL，例如：`https://your-app.railway.app`
- 复制这个 URL，稍后用于前端配置

#### 5. 配置数据持久化
- Railway 的免费层数据会持久化
- 确保 `data` 和 `uploads` 目录在项目根目录

### 第二步：部署前端到 Vercel

#### 1. 注册 Vercel 账号
- 访问 https://vercel.com
- 使用 GitHub 账号登录

#### 2. 导入项目
1. 点击 "Add New Project"
2. 选择你的 GitHub 仓库
3. 配置项目：
   - **Framework Preset**: Vite
   - **Root Directory**: `frontend`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`

#### 3. 配置环境变量
在 Vercel 项目设置中添加：
- `VITE_API_BASE_URL`: 你的 Railway 后端 URL（例如：`https://your-app.railway.app`）

#### 4. 部署
- 点击 "Deploy"
- 等待构建完成
- 获得前端 URL，例如：`https://your-app.vercel.app`

### 完成！
现在你可以通过 Vercel 的 URL 访问你的应用了！

---

## 🚀 方案二：全栈部署到 Railway

### 1. 注册并创建项目
同方案一的第一步

### 2. 配置项目
1. 根目录保持为项目根目录（不是 backend）
2. 修改 `railway.json` 或手动设置：
   - **Build Command**: `cd backend && npm install && npm run build && cd ../frontend && npm install && npm run build`
   - **Start Command**: `cd backend && npm start`

### 3. 环境变量
- `NODE_ENV=production`
- `PORT=1888`

### 4. 部署
Railway 会自动构建和部署

---

## 🚀 方案三：全栈部署到 Render

### 1. 注册 Render 账号
- 访问 https://render.com
- 使用 GitHub 账号登录

### 2. 创建 Web Service
1. 点击 "New +" → "Web Service"
2. 连接你的 GitHub 仓库
3. 配置：
   - **Name**: cross-border-workflow
   - **Environment**: Node
   - **Region**: 选择离你最近的区域
   - **Branch**: main
   - **Root Directory**: 留空（项目根目录）
   - **Build Command**: `cd backend && npm install && npm run build && cd ../frontend && npm install && npm run build`
   - **Start Command**: `cd backend && npm start`

### 3. 环境变量
- `NODE_ENV=production`
- `PORT=10000`（Render 要求）

### 4. 部署
点击 "Create Web Service"，等待部署完成

### 5. 配置前端（如果分离部署）
如果只部署后端到 Render，前端需要设置：
- `VITE_API_BASE_URL`: 你的 Render URL（例如：`https://your-app.onrender.com`）

---

## 🚀 方案四：前端部署到 Netlify

### 1. 注册 Netlify 账号
- 访问 https://netlify.com
- 使用 GitHub 账号登录

### 2. 导入项目
1. 点击 "Add new site" → "Import an existing project"
2. 选择你的 GitHub 仓库
3. 配置：
   - **Base directory**: `frontend`
   - **Build command**: `npm run build`
   - **Publish directory**: `frontend/dist`

### 3. 环境变量
在 "Site settings" → "Environment variables" 添加：
- `VITE_API_BASE_URL`: 你的后端 URL

### 4. 部署
点击 "Deploy site"

---

## 🔧 本地测试部署配置

在部署前，你可以在本地测试：

```bash
# 测试前端构建
cd frontend
npm install
npm run build

# 测试后端构建
cd ../backend
npm install
npm run build
npm start
```

---

## 📝 环境变量说明

### 前端环境变量
- `VITE_API_BASE_URL`: 后端 API 地址
  - 开发环境：`http://localhost:1888`
  - 生产环境：你的后端部署 URL

### 后端环境变量
- `NODE_ENV`: 运行环境（`production`）
- `PORT`: 服务端口（Railway/Render 会自动分配）

---

## 🐛 常见问题

### 1. 前端无法连接后端
- 检查 `VITE_API_BASE_URL` 是否正确设置
- 检查后端是否正常运行
- 检查 CORS 配置（后端已配置允许所有来源）

### 2. 构建失败
- 检查 Node.js 版本（需要 20+）
- 检查依赖是否正确安装
- 查看构建日志

### 3. 数据丢失
- Railway 和 Render 的免费层数据会持久化
- 建议定期备份 `data` 目录

### 4. 服务休眠
- Render 免费层在 15 分钟无活动后会休眠
- 首次访问需要等待几秒唤醒
- Railway 免费层不会休眠

---

## 💡 优化建议

1. **使用自定义域名**（可选）
   - Vercel 和 Netlify 都支持免费自定义域名
   - Railway 和 Render 也支持，但可能需要配置

2. **启用 HTTPS**
   - 所有平台都自动提供 HTTPS

3. **监控和日志**
   - Railway 和 Render 都提供日志查看
   - Vercel 提供 Analytics（需要升级）

4. **备份数据**
   - 定期导出 `data` 目录
   - 考虑使用 GitHub Actions 自动备份

---

## 🎉 完成！

部署完成后，你的应用就可以通过公网访问了！

**推荐配置**：
- 前端：Vercel（免费，快速）
- 后端：Railway（稳定，有免费额度）

如有问题，请查看各平台的文档或检查构建日志。

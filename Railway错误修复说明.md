# 🔧 Railway 部署错误修复

## ❌ 遇到的错误

```
sh: 1: cd: can't cd to backend
```

## 🔍 问题原因

当你在 Railway 的 Settings 中设置了 **Root Directory** 为 `backend` 后，Railway 的工作目录就已经在 `backend` 目录了。所以在构建命令中不需要再执行 `cd backend`。

## ✅ 解决方法

### 方法一：在 Railway 界面中修改（推荐）

1. **进入你的 Railway 服务页面**
2. **点击 "Settings" 标签**
3. **找到 "Build & Deploy" 部分**（或类似的配置区域）
4. **修改以下配置**：

   - **Root Directory**: `backend` ✅（保持这个）
   - **Build Command**: 改为：
     ```
     npm install && npm run build
     ```
     （删除 `cd backend &&` 部分）
   
   - **Start Command**: 改为：
     ```
     npm start
     ```
     （删除 `cd backend &&` 部分）

5. **点击 "Save" 或 "Deploy"**
6. **等待重新部署**

### 方法二：使用更新的 railway.json

我已经更新了 `railway.json` 文件。如果你想让 Railway 自动读取配置：

1. **提交更新的配置到 GitHub**：
   ```bash
   cd "/Users/maxj/Documents/Python项目/项目91-跨境电商工作流"
   git add railway.json
   git commit -m "修复 Railway 构建命令"
   git push
   ```

2. **Railway 会自动重新部署**（如果启用了自动部署）

3. **或者在 Railway 界面中手动触发重新部署**

## 📝 正确的配置

在 Railway 中设置 Root Directory 为 `backend` 后，命令应该是：

- ✅ **Root Directory**: `backend`
- ✅ **Build Command**: `npm install && npm run build`
- ✅ **Start Command**: `npm start`
- ✅ **环境变量**: `NODE_ENV=production`

## 🎯 验证修复

修复后，部署应该能够：
1. ✅ 成功执行 Build Command
2. ✅ 成功构建项目
3. ✅ 成功启动服务

## 💡 为什么会出现这个错误？

- Railway 在设置 Root Directory 后，所有命令都在该目录下执行
- 就像在终端中已经 `cd backend` 了一样
- 所以不需要在命令中再执行 `cd backend`

---

修复后重新部署，应该就能成功了！🚀
